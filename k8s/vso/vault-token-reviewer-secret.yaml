# Kubernetes populates token and ca.crt; no credential is committed here.
apiVersion: v1
kind: Secret
metadata:
  name: vault-auth-reviewer-token
  annotations:
    kubernetes.io/service-account.name: vault-auth-reviewer
type: kubernetes.io/service-account-token
